{
  "type": "object",
  "properties": {
    "text": { "type": "string", "description": "Text to display" },
    "choices": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Possible choices",
      "minItems": 0,
      "maxItems": 4
    }
  },
  "required": ["text", "choices"]
}
